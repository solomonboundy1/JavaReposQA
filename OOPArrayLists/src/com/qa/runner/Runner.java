package com.qa.runner;

import com.qa.model.Manager;

public class Runner {

	public static void main(String[] args) {
		//Create an object of my manager class
		
		Manager newManager = new Manager();
		
		newManager.addPizzas();
		
		// Using any of the constructor classes created,
		// create manager class that creates an arrayList
		// of the chosen object
		// Within the manager add two methods
		// view all objects
		// Add an object created in the manager
		
		// stretch goal, when adding objects to the arrayList,
		// pass in a parameter from the runner to add that object
		
	}

}
